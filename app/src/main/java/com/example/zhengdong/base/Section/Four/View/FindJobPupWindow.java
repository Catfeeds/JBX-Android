/* * Copyright (c) create by ki1lt0 . */package com.example.zhengdong.base.Section.Four.View;import android.annotation.SuppressLint;import android.app.Activity;import android.content.Context;import android.content.Intent;import android.graphics.drawable.ColorDrawable;import android.net.Uri;import android.util.DisplayMetrics;import android.view.KeyEvent;import android.view.LayoutInflater;import android.view.View;import android.view.View.OnClickListener;import android.view.ViewGroup;import android.webkit.DownloadListener;import android.webkit.WebChromeClient;import android.webkit.WebSettings;import android.webkit.WebView;import android.webkit.WebViewClient;import android.widget.LinearLayout;import android.widget.PopupWindow;import android.widget.RelativeLayout;import android.widget.TextView;import com.example.zhengdong.base.APIManager.UrlUtils;import com.example.zhengdong.base.Macro.DesUtils;import com.example.zhengdong.base.Macro.dzRecycleview.ProgressView;import com.example.zhengdong.jbx.R;import java.text.SimpleDateFormat;import butterknife.BindView;/** * Created by cfwifine on 16/10/28. */public class FindJobPupWindow extends PopupWindow implements OnClickListener {    private final LinearLayout mineWord;    private final LinearLayout mineHr;    private final LinearLayout mineJob;    private final LinearLayout backView;    private View mMenueView;    private Context contextx;    @SuppressLint("NewApi")    public FindJobPupWindow(Activity context, OnClickListener itemsOnclick) {        super(context);        LayoutInflater inflater = (LayoutInflater) context.getSystemService(Context.LAYOUT_INFLATER_SERVICE);        mMenueView = inflater.inflate(R.layout.find_job_pop_layout, null);        contextx = context;        mineWord = (LinearLayout)mMenueView.findViewById(R.id.mine_word_lay);        mineHr = (LinearLayout)mMenueView.findViewById(R.id.mine_hr_lay);        mineJob = (LinearLayout)mMenueView.findViewById(R.id.mine_job_lay);        mineWord.setOnClickListener(itemsOnclick);        mineHr.setOnClickListener(itemsOnclick);        mineJob.setOnClickListener(itemsOnclick);        backView = (LinearLayout)mMenueView.findViewById(R.id.alp_lay);        backView.setOnClickListener(itemsOnclick);        this.setContentView(mMenueView);        //设置SelectPicPopupWindow弹出窗体的宽        this.setWidth(ViewGroup.LayoutParams.MATCH_PARENT);        //设置SelectPicPopupWindow弹出窗体的高        this.setHeight(ViewGroup.LayoutParams.MATCH_PARENT);        //设置SelectPicPopupWindow弹出窗体可点击        this.setFocusable(true);        //设置SelectPicPopupWindow弹出窗体动画效果        this.setAnimationStyle(R.style.pop_top_in);        //实例化一个ColorDrawable颜色为半透明        ColorDrawable dw = new ColorDrawable(0xffffff);        //设置SelectPicPopupWindow弹出窗体的背景        this.setBackgroundDrawable(dw);    }    @Override    public void onClick(View view) {        switch (view.getId()) {            case R.id.mine_word_lay:                dismiss();                break;            case R.id.mine_hr_lay:                dismiss();                break;            case R.id.mine_job_lay:                dismiss();                break;            case R.id.alp_lay:                dismiss();                break;            default:                break;        }    }}